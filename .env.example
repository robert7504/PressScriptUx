# PressScript REST API base URL (server-side)
API_BASE_URL=http://localhost:8080

# Log every server-side REST call (method, URL, status, duration) to Docker/Next logs.
# Put this in .env.local (not here). Enabled automatically in development.
# API_DEBUG=true
# API_DEBUG=false
